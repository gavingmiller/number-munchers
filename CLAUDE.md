# Number Munchers

## Rules

- **Stars must always be formatted with locale-appropriate numeric separators** (thousands, millions). Use `toLocaleString('en-US')` when displaying star counts anywhere in the UI — HUD, menus, shop, history, game over screen. Never display raw unformatted numbers for stars.
