---
phase: 08-in-game-animations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/AnimationController.ts
  - specs/ui/AnimationController.spec.ts
autonomous: true
requirements:
  - ANIM-01
  - ANIM-02
  - ANIM-03
  - ANIM-04

must_haves:
  truths:
    - "AnimationController.play(key) calls sprite.play only when animation changes"
    - "AnimationController.play(key) is a no-op when same key is already playing"
    - "AnimationController.playOnce(key, cb) calls sprite.play and fires callback on completion"
    - "troggleAnimName maps each TroggleType to its correct animation name"
  artifacts:
    - path: "src/ui/AnimationController.ts"
      provides: "Animation state machine and troggle animation name mapping"
      exports: ["AnimationController", "troggleAnimName"]
    - path: "specs/ui/AnimationController.spec.ts"
      provides: "Unit tests for animation controller and troggle name mapping"
      min_lines: 60
  key_links:
    - from: "src/ui/AnimationController.ts"
      to: "AnimatableSprite interface"
      via: "minimal interface pattern (no Phaser import)"
      pattern: "interface AnimatableSprite"
---

<objective>
Create the AnimationController class and troggleAnimName helper with full test coverage.

Purpose: Build the testable animation state machine that GridRenderer will use to switch player and troggle animations. Uses the minimal interface pattern established in SpriteRegistry.ts (no Phaser import) so it runs in vitest's node environment.

Output: AnimationController.ts with tests, ready for GridRenderer integration in Plan 02.
</objective>

<execution_context>
@/Users/gmiller/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gmiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-in-game-animations/08-CONTEXT.md
@.planning/phases/08-in-game-animations/08-RESEARCH.md

<interfaces>
<!-- Minimal interface pattern from SpriteRegistry.ts — AnimationController follows same convention -->

From src/sprites/SpriteRegistry.ts:
```typescript
export interface AnimationDef {
  frames: [number, number];
  frameRate?: number;   // defaults to 8
  repeat?: number;      // defaults to -1 (loop); set 0 for one-shot
}

export function animKey(character: string, animName: string): string {
  return `${character}-${animName}`;
}
```

From src/types.ts:
```typescript
export type TroggleType = 'reggie' | 'fangs' | 'squirt' | 'ember' | 'bonehead';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AnimationController class and troggleAnimName helper with tests</name>
  <files>src/ui/AnimationController.ts, specs/ui/AnimationController.spec.ts</files>
  <read_first>
    - src/sprites/SpriteRegistry.ts (minimal interface pattern to follow)
    - src/types.ts (TroggleType union)
    - specs/sprites/SpriteRegistry.spec.ts (test style reference)
  </read_first>
  <behavior>
    - AnimationController.play('idle') calls sprite.play('idle') and sets currentAnim to 'idle'
    - AnimationController.play('idle') twice in a row only calls sprite.play once (no-op on same key)
    - AnimationController.play('walkLeft') after play('idle') calls sprite.play('walkLeft')
    - AnimationController.playOnce('munch', cb) calls sprite.play('munch') and registers once('animationcomplete', cb)
    - AnimationController.playOnce('munch', cb) when 'munch' already playing is a no-op (does not stack listeners)
    - AnimationController.getCurrentAnim() returns the current animation key
    - AnimationController constructed with null sprite — all methods are no-ops (graceful dormancy)
    - troggleAnimName('reggie') returns 'crawl'
    - troggleAnimName('fangs') returns 'slither'
    - troggleAnimName('squirt') returns 'bounce'
    - troggleAnimName('ember') returns 'flicker'
    - troggleAnimName('bonehead') returns 'stalk'
  </behavior>
  <action>
    Create `src/ui/AnimationController.ts`:

    1. Define `AnimatableSprite` interface with minimal methods:
       - `play(key: string): void`
       - `once(event: string, cb: () => void): void`
       No Phaser import — keeps class testable in vitest node environment.

    2. `AnimationController` class:
       - Constructor takes `sprite: AnimatableSprite | null` (null = dormant/programmatic fallback)
       - Private `currentAnim: string = ''`
       - `play(key: string)`: if sprite is null, return. If key === currentAnim, return. Set currentAnim = key, call sprite.play(key).
       - `playOnce(key: string, onComplete: () => void)`: if sprite is null, return. If key === currentAnim, return. Set currentAnim = key, call sprite.once('animationcomplete', onComplete), call sprite.play(key).
       - `getCurrentAnim(): string`: returns currentAnim.

    3. `troggleAnimName` exported function:
       - Takes `type: TroggleType` (import from `../types`)
       - Returns animation name string per locked decisions:
         reggie -> 'crawl', fangs -> 'slither', squirt -> 'bounce', ember -> 'flicker', bonehead -> 'stalk'
       - Uses `Record<TroggleType, string>` const map.

    Create `specs/ui/AnimationController.spec.ts`:
       - Mock AnimatableSprite with vi.fn() for play and once
       - Test all behaviors listed above
       - For null sprite test: construct with null, verify no throws on play/playOnce/getCurrentAnim
  </action>
  <verify>
    <automated>cd /Users/gmiller/work/number-munchers && npx vitest run specs/ui/AnimationController.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export class AnimationController" src/ui/AnimationController.ts
    - grep -q "export function troggleAnimName" src/ui/AnimationController.ts
    - grep -q "interface AnimatableSprite" src/ui/AnimationController.ts
    - grep -q "crawl" src/ui/AnimationController.ts
    - grep -q "slither" src/ui/AnimationController.ts
    - grep -q "bounce" src/ui/AnimationController.ts
    - grep -q "flicker" src/ui/AnimationController.ts
    - grep -q "stalk" src/ui/AnimationController.ts
    - No "import Phaser" or "import.*from.*phaser" in src/ui/AnimationController.ts
    - npx vitest run specs/ui/AnimationController.spec.ts exits 0 with all tests passing
    - bun test exits 0 (full suite green)
  </acceptance_criteria>
  <done>AnimationController class and troggleAnimName helper exist with full test coverage. All play/playOnce/no-op/dormancy behaviors verified. No Phaser import. Full test suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run specs/ui/AnimationController.spec.ts` — all AnimationController and troggleAnimName tests pass
- `bun test` — full suite remains green (no regressions)
- `grep -rL "import.*Phaser" src/ui/AnimationController.ts` — confirms no Phaser import
</verification>

<success_criteria>
- AnimationController.play() switches animations only on state change
- AnimationController.playOnce() handles one-shot animations with completion callback
- AnimationController handles null sprite (graceful dormancy for programmatic fallback)
- troggleAnimName maps all 5 TroggleTypes to their locked animation names
- All tests pass, full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/08-in-game-animations/08-01-SUMMARY.md`
</output>
